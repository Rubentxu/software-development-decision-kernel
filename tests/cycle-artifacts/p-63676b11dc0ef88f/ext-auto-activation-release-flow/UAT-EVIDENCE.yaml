# UAT-EVIDENCE — EXT auto-activation (release flow)

UAT rows verified by commit `5550fcf`.

| UAT id | Scenario | Expected invariant | Result | Evidence |
|---|---|---|---|---|
| UAT-1d-1 | No env vars set | Step 1d is a no-op; release proceeds to step 2/14. | PASS (observed in isolation) | The dry-run path through `bash scripts/release.sh --dry-run --skip-tests --skip-install` fails in **preflight** (admission needs a monotonic bump), so it does not exercise step 1d. The no-op branch logic was extracted and run in isolation: `env -u COGNICODE_MCP_BIN -u CHRONOS_MCP_BIN bash /tmp/step1d_noop_test.sh` returned `OK: no-op branch triggered, EXT tests stay #[ignore]` + `EXT_FAIL=0` + `OK: no receipt dir created (no-op)` + `exit=0`. |
| UAT-1d-2 | `COGNICODE_MCP_BIN` set, binary valid | Step 1d runs `cargo test --workspace --offline -- --include-ignored a6_cc_s1 aiw_s1_cognicode_real` with the env var propagated; on PASS, writes EXT-RECEIPT.md with `result: PASS` + test counts; on FAIL, dies. | PASS-by-code-reading (NOT observed) | `cognicode-mcp` is not installed in this env, so the active branch is `acceptance_blocked`. Code review of `scripts/release.sh` lines 294..310 confirms: cargo test invocation with env var propagation, `result: PASS`/`FAIL` written to EXT-RECEIPT.md, `EXT_FAIL=1` set on failure. Will turn into observed PASS when the operator installs the binary + sets the env var + re-runs release. |
| UAT-1d-3 | `CHRONOS_MCP_BIN` set, binary valid | Same shape as UAT-1d-2 for the Chronos EXT test files (`aiw_s5_chronos_real a7_s1_runtime_uat`). | PASS-by-code-reading (NOT observed) | Same shape; `chronos-mcp` not installed in this env. |
| UAT-1d-4 | Either env var set, EXT tests fail | Step 1d writes `result: FAIL` to EXT-RECEIPT.md and dies with `EXT tests failed against real binaries — refusing to release`. | PASS-by-code-reading (NOT observed) | Code lines 302..307 + 318..321: `EXT_FAIL=1` branch and `die` invocation are present and syntactically correct (verified by `bash -n` exit 0). Will turn into observed PASS when the operator triggers a real failure. |
| UAT-1d-5 | Step 1d ran (env vars were set) | EXT-RECEIPT.md is included as an asset in step 9/14's `gh release create` / `gh release upload` call. | PASS-by-code-reading (NOT observed) | Code at `scripts/release.sh` line ~545: `if [ -n "$EXT_RECEIPT_DIR" ] && [ -f "$EXT_RECEIPT_DIR/EXT-RECEIPT.md" ]; then ASSETS+=( "$EXT_RECEIPT_DIR/EXT-RECEIPT.md" ); fi`. The conditional is on the receipt existence, so the no-op branch correctly does NOT append (verified in isolation). |
| UAT-1d-6 | Bash syntax of modified script | `bash -n scripts/release.sh` exits 0. | PASS (observed) | `bash syntax: OK` printed. |
| UAT-1d-7 | Shellcheck of modified script | `shellcheck --severity=warning scripts/release.sh` exits 0 with no output. | PASS (observed) | (no output from shellcheck, exit 0). |

Notes:

- The 9-asset public-release gate (step 9b/14) does **not** poll
  `EXT-RECEIPT.md`. Adding the file as an asset is purely an
  evidence-of-record action; it does not change the gate's 9-asset
  contract.
- The release script still does NOT install the binaries. That is
  operator responsibility. Once `COGNICODE_MCP_BIN` is set, step 1d
  activates; until then the EXT tests stay `#[ignore]` (their default
  state since they were authored).
