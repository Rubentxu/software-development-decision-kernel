# Invalid conformance fixture — PACK009: requires ∩ conflicts_with.
[pack]
id = "fixture-invalid-conflict"
version = "0.1.0"
schema_version = 2
compatibility = ">=1.91"
risk = "low"
consequence = "creates"

[dependencies]
requires = ["sddk-core"]
conflicts_with = ["sddk-core"]

[[commands]]
name = "fixture"
surface = ["fixture"]
