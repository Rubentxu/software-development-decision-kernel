# Valid v1 conformance fixture — must exit 0 (normalizes to v2 semantics).
[pack]
id = "fixture-valid-v1"
version = "0.1.0"
schema_version = 1
compatibility = ">=1.91"
risk = "low"
consequence = "creates"

[dependencies]
required = ["sddk-core"]
optional = ["sddk-core"]

[[commands]]
name = "fixture"
surface = ["fixture"]

[fixtures]
paths = ["fixtures/plan.yaml"]
