# Valid v2 conformance fixture — must exit 0.
[pack]
id = "fixture-valid-v2"
version = "0.1.0"
schema_version = 2
compatibility = ">=1.91"
risk = "low"
consequence = "creates"
category = "domain"

[dependencies]
requires = []
integrates_with = ["sddk-core"]
conflicts_with = []

[provides]
capabilities = ["fixture.alpha"]

[[commands]]
name = "fixture"
surface = ["fixture"]

[fixtures]
paths = ["fixtures/plan.yaml"]
