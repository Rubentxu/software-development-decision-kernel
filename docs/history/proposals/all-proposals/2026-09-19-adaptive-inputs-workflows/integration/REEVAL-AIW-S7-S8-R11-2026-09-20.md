# Reevaluación AIW-S7 / AIW-S8 / R11 tras bloque J2..J6 — 2026-09-20

> Docs-only (allowlist del pre-push hook). No cierra S7/S8; registra
> la evidencia nueva y deja la decisión donde estaba (operator).

## Contexto

El bloque J2..J6 de JCODE_CORE_GA se cerró en main esta sesión
(v1.169.110..115):

- J2/J3 `agentic_session_binding.rs` (arch-spec-024) — 7 tests
- J4 `context_bridge.rs` (arch-spec-026, CDD-001..006) — 6 tests
- J5 `reactive_verify.rs` (arch-spec-025/037, AC9) — 5 tests
- J6 `structured_work.rs` (arch-spec-027, SAW-001..006) — 5 tests
- ADR-0140 documenta los módulos root nuevos.
- Workspace: 4884 PASS / 0 FAIL, clippy 0, fmt OK, hook 35/35.

## AIW-S7 (R9/UX Secretary) — sigue diferido

Requiere consulta sobre Planning snapshot reconciliado con
consumidor real y coste de consulta medido (UAT-G01..G09). Ninguno
de los módulos J2..J6 produce ese consumidor. Cerrarlo en auto-run
sigue siendo fabricación. **Decisión sigue siendo del operator.**

## AIW-S8 (J2/R11 fronteras CLI/host, packaging) — evidencia parcial nueva

Lo que S8 pide y la evidencia actual:

| Requisito S8 | Estado tras J2..J6 |
|---|---|
| Reutilizar arch-spec-030 con JCode | Los 4 módulos son host-agnostic y puros; la superficie existe, pero NO hay adapter JCode real contra ellos todavía |
| Runner+record unificado | `StructuredWorkExecutor` + `AgentExecutionReceipt` son el seed semántico; falta wiring productivo |
| Experimento segundo binario read-only | No iniciado; requiere segundo consumidor real |
| No split si duplica storage/Authority/schema | Los módulos no duplican nada (puros, sin I/O); favorable pero no concluyente |

**Conclusión S8:** la capa semántica está; el packaging-split
sigue siendo decisión basada en evidencia de consumo real, que no
existe. Pasa de PARTIAL a PARTIAL-con-superficie-completa. Sigue
requiriendo wiring con un host (Jev/JCode) para decidir.

## R11 (crate-split, P3) — sin cambios

Los 4 módulos nuevos son candidatos naturales a un crate
`sddk-agentic-api` (arch-spec-030) porque no dependen de I/O.
Moverlos es mecánico cuando el split se decida. No acción ahora.

## Recomendación

1. Siguiente inversión con ROI real: **wiring productivo de los
   módulos J2..J6 contra un host real** (adapter JCode vía
   arch-spec-030). Eso desbloquea S8 con evidencia.
2. S7 espera consumidor del Secretary; no forzar.
3. CC-S3 / A7-EXT siguen bloqueados por binarios externos
   (`cognicode-mcp`, `chronos-mcp`) activables vía release flow.
