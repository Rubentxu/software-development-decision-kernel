# Cross-context architecture

Este directorio contiene contratos que atraviesan bounded contexts: state classes, dependencia global y la arquitectura base heredada de la consolidación anterior.

Los documentos `LEGACY-*-SOURCE.md` conservan el contenido normativo anterior como referencia durante la migración. **La organización context-first de `00-SYSTEM` y `02-BOUNDED-CONTEXTS` prevalece para ownership y ubicación física nueva.**
