# Workbook — Invariants

Table: `id | statement | scope | strength | verification mode | last basis | status | waiver`.

Statuses: DECLARED, VERIFIED, VIOLATED, STALE, UNKNOWN, WAIVED.

Only explicit MUST contracts should be treated as hard violations by downstream policy.
