# Drift and staleness

Drift vector:

`source, structural, dependency, architecture, semantic, behavioral, knowledge, decision, context`.

Staleness causes are first-class so an evaluator can decide the cheapest refresh method.

A provider/analyzer version change may invalidate derived knowledge even if source is identical.
