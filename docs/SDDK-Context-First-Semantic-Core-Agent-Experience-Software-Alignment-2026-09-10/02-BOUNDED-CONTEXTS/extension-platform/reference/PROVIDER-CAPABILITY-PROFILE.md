# Provider Capability Profile

Discovery records operation versions, supported languages/adapters, limits, experimental flags, activation transport and health.

SDDK uses semantic capabilities such as `static.delta`, `static.impact`, `runtime.compare`, not assumptions about provider tool lists.
