# CLI contract mutants

Audit the supplied trace against the shared CLI contract. Report every invalid
invocation, schema assumption, hidden error, lease violation, evidence defect,
and ownership violation.
